submission.csv
data/
dataset/
outputs/tactical/

venv/
.
.venv*/

__pycache__/
.claude/
.claude//de/
outputs/v6_folds.joblib
outputs/v6_*.json
outputs/v5match.json
outputs/daily_shape.parquet
outputs/v7_*.json
outputs/v7_folds.joblib
outputs/v8_*.json
# V10 experiment artifacts: fold-model variants, per-run validation reports
# and diagnostic frames are all reproducible from tools/; everything worth
# keeping is committed under docs/. These patterns do not affect files that
# are already tracked (outputs/schedule_predictions.csv).
outputs/*.joblib
outputs/*.json
outputs/*.parquet
outputs/*.md
outputs/*.csv
outputs/*.log
outputs/*.npz
outputs/*.txt
# Dead model variants from the selection-arm experiments (rank calibration and
# the standalone resurrection gate). Superseded by the shipped v8_cens.joblib
# and pihybrid.joblib; reproducible from tools/. Named explicitly so the
# tracked shipping artifacts under models/ are untouched.
models/v7_rank.joblib
models/v8_gate.joblib
